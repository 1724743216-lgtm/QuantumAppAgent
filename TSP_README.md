# TSP量子算法应用

这是一个基于Cqlib SDK的旅行商问题(TSP)量子算法应用，支持用户自定义城市坐标，并使用QAOA算法进行求解。

## 功能特性

- **用户自定义城市**: 支持用户输入城市名称和坐标
- **距离矩阵生成**: 自动计算城市间的欧几里得距离
- **量子算法求解**: 使用QAOA算法求解TSP问题
- **经典算法对比**: 提供经典算法作为对比基准
- **可视化界面**: 简洁的Web界面，支持交互式操作

## 项目结构

```
tsp-quantum-app/
├── app.py                 # Flask应用主文件
├── tsp_model.py          # TSP问题模型定义
├── tsp_solver.py         # TSP求解器（量子+经典）
├── requirements.json     # 项目依赖
├── TSP_README.md         # 项目说明
└── static/
    ├── index.html        # Web界面
    └── style.css         # 样式文件
```

## 安装和运行

1. **安装依赖**
   ```bash
   pip install -r requirements.json
   ```

2. **启动应用**
   ```bash
   python app.py
   ```

3. **访问界面**
   打开浏览器访问 `http://localhost:5000`

## 使用说明

### 1. 定义城市
- 在"定义城市"部分输入城市名称和坐标
- 可以添加/删除城市
- 支持生成随机城市用于测试

### 2. 生成距离矩阵
- 点击"生成距离矩阵"按钮
- 系统会自动计算城市间的欧几里得距离
- 显示距离矩阵供用户查看

### 3. 选择算法参数
- 设置QAOA层数（1-3层）
- 设置量子模拟次数

### 4. 求解TSP
- **量子算法求解**: 使用QAOA算法
- **经典算法求解**: 使用经典算法
- **对比求解**: 同时运行两种算法并比较结果

### 5. 查看结果
- 显示最优路径和总距离
- 量子算法与经典算法的对比结果
- 改进程度百分比

## 技术实现

### 量子算法
- **QAOA (Quantum Approximate Optimization Algorithm)**: 用于组合优化问题
- **QUBO (Quadratic Unconstrained Binary Optimization)**: 将TSP问题转化为QUBO形式
- **量子模拟**: 使用Cqlib SDK进行量子电路模拟

### 经典算法
- **Christofides算法**: 近似求解TSP问题
- **贪心算法**: 作为备选方案

### 数学模型
TSP问题的QUBO形式：
```
minimize Σ Σ d_ij * x_ij * x_ik
```
其中：
- `x_ij` 是二元变量，表示城市j是否在位置i
- `d_ij` 是城市i和j之间的距离
- 约束条件确保每个城市只被访问一次，每个位置只有一个城市

## API接口

### POST /api/cities
定义城市并生成距离矩阵
```json
{
  "cities": [
    {"name": "北京", "x": 0, "y": 0},
    {"name": "上海", "x": 100, "y": 0}
  ]
}
```

### POST /api/solve
使用量子算法求解TSP
```json
{
  "distance_matrix": [[0, 100], [100, 0]],
  "num_cities": 2,
  "p": 1
}
```

### POST /api/classical
使用经典算法求解TSP
```json
{
  "distance_matrix": [[0, 100], [100, 0]],
  "num_cities": 2
}
```

## 限制和注意事项

1. **量子算法限制**:
   - 当前使用量子模拟，实际量子硬件需要修改后端
   - QAOA层数建议不超过3层
   - 城市数量建议不超过8个（受量子比特数限制）

2. **算法精度**:
   - 量子算法是近似算法，可能无法找到最优解
   - 经典算法提供基准对比

3. **性能考虑**:
   - 量子模拟计算时间较长
   - 大规模问题建议使用经典算法

## 扩展功能

1. **支持实际量子硬件**:
   - 修改tsp_solver.py中的模拟器配置
   - 连接真实的量子计算后端

2. **更多算法**:
   - 添加其他量子算法（如VQE、QAOA变体）
   - 增加更多经典算法

3. **可视化增强**:
   - 添加路径可视化
   - 实时显示算法进度

4. **性能优化**:
   - 优化QUBO矩阵构建
   - 改进量子电路设计

## 许可证

本项目仅供学习和研究使用。